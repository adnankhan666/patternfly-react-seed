import * as React from 'react';
import { Placeholder } from '@app/Placeholder/Placeholder';

const ModelRegistry: React.FunctionComponent = () => (
  <Placeholder title="Model Registry" description="Register and version your ML models" />
);

export { ModelRegistry };
